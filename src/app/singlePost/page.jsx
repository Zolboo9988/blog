import React from "react";
import { SinglePost } from "@/components/SinglePost";
export default function PageComponent({ sing }) {
  return (
    <div>
      <SinglePost sing={id} />
    </div>
  );
}
